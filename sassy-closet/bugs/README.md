# Color-detect fixtures

Expected chips after upload (auto-fill, no Suggest):

| Photo | Dominant | Also OK |
| --- | --- | --- |
| `sample-cream-cardigan.png` | **Kem** | Caro if gingham tiles fire |
| `sample-red-cardigan.png` | **Đỏ** | Trắng / Caro only if those pixels are real |

Ignore studio couch (Đen) and the wood floor strip (Be/Nâu).

The PNGs here are layout stand-ins for the two Boss/GF cardigan photos (cream + gingham collar, red + white embroidery). Drop the camera originals in this folder with the same names when you have them — the prove script uses whatever is on disk.
