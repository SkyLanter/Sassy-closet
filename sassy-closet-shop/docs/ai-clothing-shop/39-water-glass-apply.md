# Official #39 APPLY — Apple water glass PDP (Fast OFF, 1B)

Quiet apply of [SkyLanter/Sassy-closet#39](https://github.com/SkyLanter/Sassy-closet/pull/39) `18-visual-fx-apple-water-glass.md` **§19**. Kelly Ying paper/ink/gold stays. Featured watery pour stays **320ms**. Excel / OneDrive stay read-only. No Swiper. No invented `001.jpg`.

Source playbook: [18-visual-fx-apple-water-glass.md](./18-visual-fx-apple-water-glass.md).

| §19 fix | Landed |
| --- | --- |
| 1. Same JPEG | Color chips no longer remount `AnimatePresence` keyed on `colorId`. Rail is one this-mã reel. Chip outline can move while `cover.jpg` stays. Start `colorId = null` (no auto-first hex). Tap again clears the chip. |
| 2. Color → roll | `productGalleryReel` + `firstReelIndexForColor`. Kem/Xanh on A01 **scroll** to tagged slides (shared `null` if that is all this mã has). Empty color = chip + Message line, never A01 hexes on A02/P02, never a cloned cover. |
| 3. Edge frost | `.ky-gallery-edge` sibling strips (paper mix + `blur(12px)`), only on the neighbor side. Not on `<img>`. `prefers-reduced-transparency` → opaque paper. Fast OFF keeps the 78% center-snap peek. |
| 4. APG | Peeking rail is `region` / `carousel` without that word in the label. Slides are groups. Prev/next 44×44. Dots ≥24px hit. No wrap. No secret img cycle (click still opens lightbox). |
| 5. VT name | Still `product-{THIS mã}` on the stable 3/4 frame. Related tiles keep their own names. |
| 6. Reduce | Instant scroll; snap off; mask off; frost static unless transparency is reduced. |

## Deliberately not applied

- LEARN 18’s live hex discs / `ColorSwatch` (this shop already ships text chips).
- `flex: 0 0 86%` (Fast OFF keeps the 78% / 12px peek from #36/#38).
- Inventing `/products/{MA}/001.jpg` so a one-photo mã can peek.
- iOS Liquid Glass / displacement / goo on photography.
- Customer Hold / Available copy.

Gate: `npm run smoke:motion` plus `smoke:pdp` / `smoke:media` / `smoke:bugcheck`.
