# PDP color · size · Hold (APPLY)

Learn note from Official PR #19, applied on this sell-test only. **Do not redesign** Kelly Ying chrome.

A PDP is look + choose + inbox mã. Unique piece = unique mã. Display the assigned hub code (A01). Never invent a mã. Never translate to Official `AO001`.

## Colors

- **Admin:** named colors + optional per-color photos. Hex discs stay in admin. Names required. Photos do **not** block Save.
- **Customer:** text-only color pills. No hex disc as the only cue. Picking a name filters **this mã’s** gallery: tagged-to-color ∪ untagged/shared.
- Empty view = **—**. Never another mã. Never a CSS circle.
- Echo: `Đang xem: {name}`. `color.note` is a visible line (Hoa / Caro), not tooltip-only.
- Keep existing motion and add a paper/gold lightbox (Esc / backdrop / 3-up thumbs +N).

## Size

- Asia letters only: `2XS XS S M L XL 2XL`. Buttons, never a US dropdown.
- Cm sheet shows **stored** bust/chest, waist, length only (`88 cm`). Do not invent cm from a letter.
- No letter / no cm: honest empty — `Inbox {MA} for fit`. Hide the letter row for PK / hair / thermos when empty.

## Hold · dropship

- This shop: `available | hold | sold` only.
- Hold → **Tạm giữ · Hold** + Inbox for price / `no USD yet`.
- Available dropship → Message-first, not warehouse InStock.
- Sold → Sold. This mã is gone.
- Do not print Square / On_Hand theater on the public shop.

## Not applied (non-goals)

Shopify widget, US size chart, hex-only customer swatches, fake cm, fake reviews, AR, sibling mãs, countdown, Square Save, intake rose restyle.
